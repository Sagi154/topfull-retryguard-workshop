# Phase 1 — GCP VMs (project: networks-workshop, zone: us-central1-a)
# Machine type: e2-standard-8 (8 vCPU, 32 GB). Ubuntu 20.04.
# Public IPs change if VMs are stopped/started — refresh with:
#   gcloud compute instances list --project=networks-workshop

MASTER_NAME=topfull-master
MASTER_PRIVATE_IP=10.128.0.3
MASTER_PUBLIC_IP=35.255.159.134

WORKER1_NAME=topfull-worker-1
WORKER1_PRIVATE_IP=10.128.0.4
WORKER1_PUBLIC_IP=34.28.248.182

LOADGEN_NAME=topfull-load
LOADGEN_PRIVATE_IP=10.128.0.2
LOADGEN_PUBLIC_IP=136.115.39.214

SSH_USER=idozacharia
# ssh -i ~/.ssh/id_ed25519 idozacharia@$MASTER_PUBLIC_IP
