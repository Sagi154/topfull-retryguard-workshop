# AGENTS.md — Context Guide for AI Agents

> **Purpose:** This file orients a new agent session on this repo: what the project is, what has been built, what state it's in *right now*, and what's left to do. Read this first, then follow the links for details. Don't duplicate this file's content elsewhere — update it as the project progresses. Domain vocabulary: [CONTEXT.md](CONTEXT.md). Architecture decisions: [docs/adr/](docs/adr/).

---

## 1. What this project is

**TAU Deepness Lab Workshop — "Project 1: Retries for Cloud Microservices."**

Goal: evaluate **RetryGuard** (a retry-storm controller we implement ourselves from its paper) on top of **TopFull** (an existing overload-control system, from KAIST) running the **Online Boutique** microservice demo app on a real Kubernetes cluster, under **Istio** service mesh, with **Locust** generating load.

Two experimental conditions, same load each time:

| Condition | Overload control | Retries |
|---|---|---|
| **Baseline** (Phase 5) | TopFull | Istio default (`attempts: 3`, RetryGuard off) |
| **Primary** (Phase 6) | TopFull | RetryGuard dynamically toggles retries per service |

We run **6 scenarios** (see §5): the eval-deck five plus Scenario 6 (forced recovery, so re-enable can be measured without changing S2's load). S1–S4 and S6 run under both conditions, ≥3 repeats each (Locust traffic is non-deterministic); S5 is RetryGuard-only and uses **S6's load**, compared against S6 baseline — not S2. Then compare goodput / P95 latency / rejection rate / retries-per-request / CPU/memory / RetryGuard toggle events. The finished August **38-run matrix** has goodput/P95/rejection but not retries or CPU/memory, and its 8 S5 runs never re-enabled. **Phase 7 close-out is a completed paper-grade 48-run campaign** (new slots, all collectors on, ×3 including S5) — see [PHASE7-RESOLVE-GAPS-1-3.md](Guides%20and%20Info/PHASE7-RESOLVE-GAPS-1-3.md). P99 was dropped as a target metric (P95 fully satisfies both source papers and the eval deck) — see [PHASE7-DATA-GAPS.md](Guides%20and%20Info/PHASE7-DATA-GAPS.md).

This is a workshop deliverable: a presentation (project plan) + eventually a written report with the above comparison.

---

## 2. Repo layout

```
Guides and Info/     ← all human-facing docs (see §3 — start here for "why")
  mentor-update/     ← mentor-facing dry-results doc + curated/gallery PNGs ([MENTOR-UPDATE.md](Guides%20and%20Info/mentor-update/MENTOR-UPDATE.md))
experiments/         ← the actual tooling: runner, scenario configs, RetryGuard + Envoy retry collectors
  configs/           ← 16 scenario YAML files (S1–S4 baseline/RG, S5×4 intervals, S6 recovery baseline/RG)
  run_scenario.py    ← orchestrator, runs on Windows, drives everything over SSH
  retryguard.py      ← RetryGuard controller (deployed to master)
  envoy_retry_collector.py ← Gap 3: scrapes Envoy sidecar outbound retry counters (deployed to master)
  resource_usage_collector.py ← Layer 2: scrapes CPU/memory per service via kubelet stats/summary (deployed to master)
  mentor_charts.py / mentor_charts_data.py / mentor_charts_plots.py ← regenerate mentor-update PNGs from `campaign_48/` (`python experiments/mentor_charts.py`)
  virtual-services.yaml ← Istio VirtualService manifests (retries.attempts: 3 default)
  patch_metric_collector.py ← one-shot patcher already applied on master (see §6d in PHASE5 guide)
  results/           ← local results only (master still uses a flat `/home/idozacharia/experiments/results/`)
    campaign_48/     ← **primary Phase 7 dataset** — 48-run campaign (2026-09-05 → 2026-09-06), in git, organized into 7 scenario subfolders (`S1_normal_op/` … `S6_forced_recovery/`) — see [experiments/results/campaign_48/README.md](experiments/results/campaign_48/README.md)
    august_38/       ← historical August 38-run matrix (git-tracked; do not delete; still a flat list of run folders, not reorganized)
TopFull/             ← git submodule, upstream https://github.com/kaist-ina/TopFull (read locally; also cloned separately on the VMs)
context/             ← papers/decks: RetryGuard.pdf, TopFull.pdf, Evaluating_RetryGuard_on_TopFull.{pdf,md} (current eval deck + transcription)
canvases/            ← interactive workplan canvas for Cursor (topfull-retryguard-workplan.canvas.tsx)
infra/vm-ips.env     ← a snapshot of VM IPs/user from one teammate's session — likely STALE, see §7
.cursor/rules/topfull-ssh.mdc ← always-applied rule: SSH conventions for the 3 VMs
scripts/             ← md_to_pdf.py, sync-canvas-to-cursor.ps1 (presentation/canvas tooling, not experiment-critical)
```

**Submodule note:** `TopFull/` is a git submodule (`.gitmodules`). If it appears empty, run `git submodule update --init`.

---

## 3. Guides — what to read for what

All in `Guides and Info/`. Read in roughly this order depending on task:

| Doc | Read when you need to... |
|---|---|
| [PREREQUISITES.md](Guides%20and%20Info/PREREQUISITES.md) | Understand what should be true before any cloud work (mostly historical now — VMs already exist) |
| [MENTOR-COORDINATION.md](Guides%20and%20Info/MENTOR-COORDINATION.md) | Understand decisions mentors already made (GCP, Istio-based RetryGuard integration, eval strategy) |
| [WORKPLAN.md](Guides%20and%20Info/WORKPLAN.md) | See the full Phase 0–7 plan with Why/How/Done-when for each step (Phases 0–6 done; Phase 7 campaign data complete; 7a/7b eval + written report remaining) |
| [SETUP-GUIDE.md](Guides%20and%20Info/SETUP-GUIDE.md) | Get exact commands for cluster/Istio/dependency setup, if something needs to be rebuilt |
| [MANAGED-KUBERNETES.md](Guides%20and%20Info/MANAGED-KUBERNETES.md) | Understand why we used self-managed `kubeadm` K8s instead of GKE (decision already made — not adopted) |
| [CONNECT-VMS.md](Guides%20and%20Info/CONNECT-VMS.md) | **SSH into the VMs.** Canonical playbook — also mirrored in `.cursor/rules/topfull-ssh.mdc` (always-applied) |
| **[PHASE5-EXPERIMENTS-GUIDE.md](Guides%20and%20Info/PHASE5-EXPERIMENTS-GUIDE.md)** | Canonical reference for the experiment runner + scenario config system. Read before touching `experiments/` |
| **[PHASE5-PHASE6-RUNLIST.md](Guides%20and%20Info/PHASE5-PHASE6-RUNLIST.md)** | **Matrix checklist.** All 38 runs done as of 2026-08-15; all 38 folders are local and in git. The “where results live” header in that file is stale |
| [EXPERIMENT-READINESS-WORKPLAN.md](Guides%20and%20Info/EXPERIMENT-READINESS-WORKPLAN.md) | Historical smoke-validation checklist (Steps 1–8 passed 2026-08-07). Do not mix smoke folders into Phase 7 analysis |
| [METRICS-COLLECTION-GUIDE.md](Guides%20and%20Info/METRICS-COLLECTION-GUIDE.md) | What each run produces, how to pull/verify, how to load for charts |
| [TOPFULL-HOW-THEY-MEASURE.md](Guides%20and%20Info/TOPFULL-HOW-THEY-MEASURE.md) | Plain-language notes from the TopFull paper: goodput, SLO, and the charts they use to score the system |
| **[METRICS-GATHERED.md](Guides%20and%20Info/METRICS-GATHERED.md)** | **Inventory of every metric we actually collect** (Locust CSVs, Envoy retries, CPU/memory, TopFull throttle, RetryGuard log, empty/`Latency99` caveats, clocks). Start here for “what do we gather?” |
| **[METRICS-CATALOG.md](Guides%20and%20Info/METRICS-CATALOG.md)** | **Why we collect each metric** — same coverage as METRICS-GATHERED.md, organized by "vantage point in the stack" with a rationale per metric and a metric→open-question map. Read this for report-writing / justifying why a number is in the paper. |
| [TOPFULL-THROTTLE-METRICS.md](Guides%20and%20Info/TOPFULL-THROTTLE-METRICS.md) | **Implemented collector** (`topfull_throttle.csv` + `topfull_detect.csv`). No campaign data yet — `campaign_48/` / `august_38/` still lack these files. |
| [PER-SERVICE-METRICS.md](Guides%20and%20Info/PER-SERVICE-METRICS.md) | Locust is per **entry API**, not per Kubernetes service. What is already per-service (CPU, some Envoy retries) vs what Envoy inbound could add |
| [PER-SERVICE-MESH-COLLECTOR-DESIGN.md](Guides%20and%20Info/PER-SERVICE-MESH-COLLECTOR-DESIGN.md) | **Implemented (2026-09-08).** Full-mesh Envoy collector (`service_edges.csv` / `service_inbound.csv`) + `service_capacity.json`; no campaign data yet |
| **[PHASE7-DATA-GAPS.md](Guides%20and%20Info/PHASE7-DATA-GAPS.md)** | **Read before analysing.** August-38 audit plus campaign close-out: Gap 1/3 **closed** on `campaign_48/`; Gap 2 dropped P99 (use P95). |
| **[PHASE7-RESOLVE-GAPS-1-3.md](Guides%20and%20Info/PHASE7-RESOLVE-GAPS-1-3.md)** | **Runbook.** Paper-grade 48-run campaign — **COMPLETE** (2026-09-06). Primary analysis is `experiments/results/campaign_48/`. Do not use `run_all_scenarios.py`. |
| [Evaluating_RetryGuard_on_TopFull.md](context/Evaluating_RetryGuard_on_TopFull.md) | **Eval plan deck** (transcription of the PDF). Slides 8–9 open questions, 11–15 scenario objectives, 16 measurement layers — source of truth for what Phase 7 is supposed to answer |
| [SCENARIOS-GUIDE.md](Guides%20and%20Info/SCENARIOS-GUIDE.md) | Understand each of the 6 scenarios in detail: what it tests, load setup, manual step-by-step (pre-dates `run_scenario.py` automation, but good for *why*) |
| **[MENTOR-UPDATE.md](Guides%20and%20Info/mentor-update/MENTOR-UPDATE.md)** | **Mentor-facing dry results** (infra, Online Boutique, scenarios, per-scenario charts + observations). Pipeline / leftover caveats: [mentor-update/README.md](Guides%20and%20Info/mentor-update/README.md) |
| **[RON-NEZER-SETUP-VS-WORKSHOP.md](Guides%20and%20Info/RON-NEZER-SETUP-VS-WORKSHOP.md)** | **The 2026-09-20 audit.** Copied his VMs, not his experiment setup. `/home/user/TopFull` is Ron's 2025 recipe (load mix, detector quotas, HPA, custom images); live runs use `/home/idozacharia/TopFull` (KAIST + our patches). |
| **[RON-NEZER-BASE-MIGRATION.md](Guides%20and%20Info/RON-NEZER-BASE-MIGRATION.md)** | **Narrative of the migration onto Ron's setup as the experiment base.** Where we differed, what we changed (CPU/replica/HPA + loadgen shape), what was left out, what's still open. Specs/plans/ADRs are linked from there — start here, not in the task logs. Remaining-work checkboxes: [2026-09-21-s1-s6-loadgen-numbers-remaining-work.md](docs/superpowers/specs/2026-09-21-s1-s6-loadgen-numbers-remaining-work.md). |
| [RETRYGUARD-IMPLEMENTATION.md](Guides%20and%20Info/RETRYGUARD-IMPLEMENTATION.md) | Understand exactly how `experiments/retryguard.py` maps to paper Algorithm 1, its metric source, endpoint→service map, VS patch mechanics, log format |
| [NEXT-PHASES-PLAN.md](Guides%20and%20Info/NEXT-PHASES-PLAN.md) | See the presentation-track vs lab-track split and near-term suggested order |
| [PRESENTATION-GUIDE.md](Guides%20and%20Info/PRESENTATION-GUIDE.md), [PRESENTATION-ACTION-ITEMS.md](Guides%20and%20Info/PRESENTATION-ACTION-ITEMS.md), [NOTEBOOKLM-PROMPT.md](Guides%20and%20Info/NOTEBOOKLM-PROMPT.md) | Work on the slide deck / presentation track (separate from the lab track) |
| [GEMINI-PROMPT.md](GEMINI-PROMPT.md), [CANVAS-VIEWING.md](CANVAS-VIEWING.md) | Presentation-generation tooling and how to view the interactive canvas in Cursor |

---

## 4. Current status (as of 2026-09-21)

### ✅ Done

- **Phases 0–4** (accounts, VM provisioning, K8s cluster via `kubeadm`, Istio 1.17 minimal profile, dependencies, Online Boutique deployed with Envoy sidecars, `instance_scaling.py` run) — complete.
- **Experiment infrastructure** — `run_scenario.py`, `run_all_scenarios.py` (38-slot matrix), 16 YAMLs (S1–S6), RetryGuard, VirtualServices, metric_collector fix — complete (PHASE5-EXPERIMENTS-GUIDE.md §6).
- **Experiment readiness (smoke)** — Steps 1–8 passed 2026-08-07. Smoke data is **not** matrix data.
- **Phase 5 & 6 matrix (38 runs)** — complete as of 2026-08-15:
  - Slots 1–9 (S1 base×3, S1 RG×3, S2 base×3): run 2026-08-11 (Ido).
  - Slots 10–38: resumed with `run_all_scenarios.py --yes --resume 9` on 2026-08-15 (~7h); slot 17 (S3 RG run2) re-run after a permission failure on a stale master folder.
  - Batch runner bumped touched configs to next free `run_number` (typically 4 for ×3 scenarios, 3 for S5).
- **Results consolidated** — August 38 live locally under `experiments/results/august_38/` **and tracked in git** (`90c43c9` on 2026-08-20 added S1×6 and S2 baseline×3). Older “S1 on Ido’s machine / S2 baseline on master only / 29 local folders” notes are stale.
- **Results audit (2026-08-20)** — [PHASE7-DATA-GAPS.md](Guides%20and%20Info/PHASE7-DATA-GAPS.md). **30 of 38 runs are usable** (all except the 8 Scenario 5 interval runs). Three gaps found: (a) **zero `OFF→ON` events in all 23 RetryGuard runs**, so S5 has no signal and S2 never completed disable→recover→re-enable under a flat hold; (b) **`Latency99` is 0 in every row** — **resolved: P99 dropped**, report P95; (c) **no retries-per-request series** in the finished matrix — collector built, needs re-runs. Layer 2 CPU/mem was unwired in the 38-run matrix — **now instrumented** via `resource_usage_collector.py`.
- **S2 vs S6 split (2026-09-01)** — a 2026-08-20 edit had put the 900s load-drop onto S2, diverging from the deck. **S2 is again a flat 600s hold** (`run_number: 4`). The load-drop is **Scenario 6** (`scenario_6_recovery_{baseline,retryguard}.yaml`). S5 stays on S6's load and compares against **S6 baseline**, not S2. See [SCENARIOS-GUIDE.md](Guides%20and%20Info/SCENARIOS-GUIDE.md) Scenario 6.
- **Paper-grade campaign chosen (2026-09-04)** — close Gaps 1 and 3 with a **new 48-run matrix**, not a replay of the August 38 and not a 16-run collector add-on. S1, S2, S3, S4A, S4B, S6 × both arms × 3 (36) plus S5 × 4 intervals × 3 (12). All collectors on, next free `log_folder` slots. Primary Phase 7 analysis uses that campaign; the August 38 stays historical. Exact slots, gate on first S6 `OFF→ON`, and verification: [PHASE7-RESOLVE-GAPS-1-3.md](Guides%20and%20Info/PHASE7-RESOLVE-GAPS-1-3.md).
- **Eval deck transcribed** — `context/Evaluating_RetryGuard_on_TopFull.md` (from the PDF). Replaces the older project-plan pptx files.
- **Envoy retry collector (Gap 3, 2026-08-20)** — scrapes caller-side Envoy outbound retry counters via `kubectl exec` into `istio-proxy`; writes `envoy_retries_{frontend,checkoutservice}.csv`. Present in all 48 `campaign_48/` folders; **absent** from `august_38/`.
- **Resource usage collector (Layer 2)** — `experiments/resource_usage_collector.py` scrapes per-service CPU/memory via kubelet `stats/summary`; wired into `run_scenario.py`. Present in all 48 `campaign_48/` folders; **absent** from `august_38/`.
- **Pre-campaign metric verification PASSED (2026-09-04)** — S6 baseline + RetryGuard run1 with traffic. Folders: `baseline_topfull_no_retryguard_forced_recovery_run1`, `run_topfull_retryguard_forced_recovery_run1`. Confirmed: Locust 1s CSVs; Envoy `max_retry>0` on baseline frontend; `resource_usage.csv`; 3× `ON→OFF` then 3× `OFF→ON` on RetryGuard (Gap 1 signal live). See [PHASE7-RESOLVE-GAPS-1-3.md](Guides%20and%20Info/PHASE7-RESOLVE-GAPS-1-3.md) §3b.
- **Paper-grade 48-run campaign COMPLETE (2026-09-05 → 2026-09-06)** — all 48 slots local under `experiments/results/campaign_48/`: S6 run1–3 both arms; S5 intervals run3–5 (×4); S1–S4A/S4B run4–6 both arms. Every run pulled and §4-verified (Locust + Envoy + `resource_usage`; RetryGuard toggles where applicable). S6/S5 produced `OFF→ON`; flat S2/S3/S4 typically disable-only. S1 RG showed unexpected checkout `ON→OFF` on all 3 repeats. August 38 is `experiments/results/august_38/`. Primary Phase 7 analysis uses the campaign. Gaps 1 and 3 **closed** on that dataset — [PHASE7-DATA-GAPS.md](Guides%20and%20Info/PHASE7-DATA-GAPS.md).
- **Local results layout (2026-09-06)** — `experiments/results/campaign_48/` vs `experiments/results/august_38/`. Both are in git. Master still uses a flat `/home/idozacharia/experiments/results/<log_folder>/`. See [experiments/results/README.md](experiments/results/README.md).
- **Mentor update doc (2026-09-06 → 2026-09-07)** — [MENTOR-UPDATE.md](Guides%20and%20Info/mentor-update/MENTOR-UPDATE.md) is the dry Phase 7 readout (infra, Online Boutique, scenarios, per-scenario charts with factual observations, no conclusions). Charts come from `campaign_48/` via `python experiments/mentor_charts.py`: Locust comparison plots are **mean-only** (no min/max bands), downsampled to **5 s** steps, with `ON→OFF` / `OFF→ON` overlays from **every** RetryGuard repeat; every scenario section embeds the S2-style set (goodput / P95 / rejection / frontend retries-per-target / CPU / memory). Pipeline leftovers (collector clocks ≠ Locust t0; no toggle overlays on CPU/retries multi-line charts; Locust `charts_gallery/` = curated) live in [mentor-update/README.md](Guides%20and%20Info/mentor-update/README.md) — not in the mentor doc.
- **Full-mesh Envoy collector (2026-09-08, transport 2026-09-13)** — `experiments/envoy_retry_collector.py` writes `service_edges.csv` / `service_inbound.csv` (all 11 Boutique services, outbound + inbound); `run_scenario.py` widens stats-inclusion to all Deployments and snapshots `service_capacity.json` **after** constraints (`kubectl get deploy` CPU limits — S3 checkout is 100m in that file, S1 is 1000m). The scrape loop runs on `topfull-master` (`transport: network_prometheus`): each poll GETs `http://<pod_ip>:15020/stats/prometheus`; CSVs land in `record_path` during the run, so RetryGuard's `measure_value()` can read inbound live. Cool S1 credit pair (run18 both-off / run19 mesh-on) measured ~**+37 ms** getcart P95 tax. The original 48-run `campaign_48/` matrix rows and `august_38/` still have only legacy `envoy_retries_{frontend,checkoutservice}.csv`. First pulled full-mesh folders under that tree are the 2026-09-13 checkpoint holds (S1 run20, S2 run17) — see below. **`mentor_charts.py` still only knows Locust** (and the legacy `envoy_retries_{caller}.csv` shape). Wiring `service_inbound.csv` / `service_edges.csv` into plots is a follow-up we should do — not a claim that mesh is not an outcome. Those files are already intended as per-service outcomes / insights ([PER-SERVICE-METRICS.md](Guides%20and%20Info/PER-SERVICE-METRICS.md)); RetryGuard now reads inbound as `measure_value()` ([2026-09-10-retryguard-mesh-measure-value-design.md](docs/superpowers/specs/2026-09-10-retryguard-mesh-measure-value-design.md)).
- **TopFull throttle collector (2026-09-09)** — `experiments/topfull_throttle_collector.py` writes `topfull_throttle.csv` (Layer A cap/admitted) and `topfull_detect.csv` (Layer B detector reconstruction). Wired into `run_scenario.py` and all 16 scenario YAMLs (`enabled: true`, 1 s). **No campaign-matrix data yet** — the original 48-run / `august_38/` folders lack these files. The 2026-09-13 S1 run20 / S2 run17 checkpoint folders have them. `mentor_charts.py` does not read them.
- **TopFull quota↔K8s sync (2026-09-10)** — paper CPU table + `cpu_limit_fraction` in S3/S4; runner reconciles K8s to paper, writes `topfull_run_quotas.json`, patches Detector; Layer B uses the same map. Design: `docs/superpowers/specs/2026-09-09-topfull-quota-k8s-sync-design.md`.
- **RetryGuard controller made paper-exact (2026-09-10)** — `experiments/retryguard.py`'s Algorithm 1 implementation now matches the paper literally: `sample_interval_seconds` (=1, raw 1s samples, no averaging) + a single symmetric `interval_samples` (paper default 30) applied to both the disable and re-enable transitions, replacing the old `window_duration_seconds`/`disable_windows`/`re_enable_windows` asymmetric-and-averaged workshop extension. Updated everywhere the old params appeared: `run_scenario.py`, all 16 scenario YAMLs, `RETRYGUARD-IMPLEMENTATION.md`, `experiments/README.md`, `PHASE5-EXPERIMENTS-GUIDE.md`, `SCENARIOS-GUIDE.md`, `METRICS-COLLECTION-GUIDE.md`. New unit tests in `experiments/test_retryguard.py` cover the state machine and metric reader. **`campaign_48/` predates this change and does not reflect it** — per explicit instruction, it is left as-is (out of date, to be superseded by a new campaign run later); do not use it to validate the new controller's behavior.
- **Metrics docs refreshed + new catalog (2026-09-11)** — [METRICS-GATHERED.md](Guides%20and%20Info/METRICS-GATHERED.md), [METRICS-COLLECTION-GUIDE.md](Guides%20and%20Info/METRICS-COLLECTION-GUIDE.md), and [PER-SERVICE-METRICS.md](Guides%20and%20Info/PER-SERVICE-METRICS.md) corrected against the live `envoy_retry_collector.py`/`topfull_throttle_collector.py`/`resource_usage_collector.py`/`run_scenario.py` code (full-mesh collector fully supersedes the legacy 2-caller CSVs rather than adding to them; `topfull_detect.csv`'s `cadvisor_cpu` is a distinct cAdvisor DaemonSet scrape, not kubelet `resource_usage.csv`; `quota` tracks the live paper+fraction quota-sync map, not a stale hardcoded table; `service_capacity.json`/`topfull_run_quotas.json`/manifest quota fields documented). New [METRICS-CATALOG.md](Guides%20and%20Info/METRICS-CATALOG.md) gives a rationale ("why") per metric, organized by vantage point in the stack, plus a metric→open-question map — still no campaign data for the mesh/throttle collectors.
- **RetryGuard `measure_value()` is mesh inbound (2026-09-10)** — `experiments/retryguard.py` now reads `service_inbound.csv` (`Δ5xx / Δtotal`, no 4xx, no double-count) for the 9 HTTP backends in `CONTROLLED_SERVICES` (includes `paymentservice`; excludes `frontend` / `redis-cart`). Locust CSVs are storefront outcomes only. Spec: `docs/superpowers/specs/2026-09-10-retryguard-mesh-measure-value-design.md`. `campaign_48/` predates this (Locust-driven) and stays historical. `mentor_charts.py` still only knows Locust; wiring mesh into plots remains a follow-up — mesh is still an intended per-service outcome.
- **S1/S2 baseline metric checkpoint (2026-09-13)** — full-duration collector-stack holds, **not** campaign-matrix repeats, stored under the `campaign_48/` tree: `S1_normal_op/baseline_topfull_no_retryguard_normal_op_run20` (300 s, **PASS**) and `S2_sustained_overload/baseline_topfull_no_retryguard_sustained_overload_run17` (600 s, **FAIL on retry gates only**). Mesh `2xx` live (`transport: network_prometheus`). S1 getcart Fail 0.79 / P95 906.8 vs run10 1.54 / 900.4 (leftover collector tax ~**+6 ms**). S2 getcart Fail 161.17 / P95 2197.8; inbound/outbound **5xx = 0** so Envoy `retry` stayed 0 (measurement finding, not a dead collector). Collector stack is usable for goodput / P95 / rejection / CPU / mesh 2xx-5xx / Layer A-B (per-service max util / overloaded in that spec §6). **Not** ready to claim a retries-per-request storm from these two folders. Spec: [2026-09-13-s1-s2-baseline-metric-checkpoint.md](docs/superpowers/specs/2026-09-13-s1-s2-baseline-metric-checkpoint.md). Baseline YAMLs bumped to S1 **run21** / S2 **run18**. The original 48-run matrix rows are unchanged.
- **§7b perTryTimeout fix (2026-09-14)** — `perTryTimeout: 500ms` added to all Boutique VirtualServices (`virtual-services.yaml`). `run_scenario.py` applies it at run start via `apply_per_try_timeout()` (both arms). `retryguard.py` includes it when re-enabling. Top-level YAML `retries:` block (`attempts_on` / `attempts_off` / `per_try_timeout_ms: 500`) in all 16 scenario configs; `REQUIRED_PARAMS` enforces `per_try_timeout_ms`. Failure signal widened to `Δ(5xx + resets) / Δtotal` (`downstream_rq_rx_reset` → `resets` column). `campaign_48/` predates this fix and stays historical.
- **§7b verification (2026-09-14)** — S2 baseline `…_run19` + RetryGuard `…_run9` under `campaign_48/S2_sustained_overload/` (5 min cool-off between). **Baseline PASS:** `service_edges.csv` max outbound `retry` to checkoutservice **3** (any-target max **46**); `service_inbound.csv` max `resets` on checkoutservice **3** (frontend **428**). **RetryGuard FAIL on toggle gate:** `retryguard.log` has **zero** `ON→OFF` / patches — max `high` streak was **1** (checkoutservice once at rejection 0.333); only **6** OBSERVE samples had rejection > 0 across 5013 observations. Retries/resets fire, but `Δ(5xx+resets)/Δtotal` almost never stays above 0.20 for `interval_samples=30`. S2 YAMLs bumped to next free slots **run20** / **run10**.
- **S1/S2 baseline readout on e2-standard-8 (2026-09-15)** — VMs had been upsized to 32/32/16 and could not start under `CPUS_ALL_REGIONS=32`; resized all three back to **`e2-standard-8`**, then ran S1 baseline **run21** (300 s) + 300 s cool-off + S2 baseline **run20** (600 s wall). Full METRICS-GATHERED file set present both folders. **S1 degraded vs run20** (getcart Fail 69.83 / P95 1696 — not a cool baseline on 8 vCPU). **S2 collectors truncated** (~282 Locust rows; mesh/throttle SHUTDOWN ~2.5 min after Locust start with Layer A timeouts) — not a full 600 s series. `perTryTimeout` **did** produce retries/resets (frontend→recommendationservice Δretry **+166907** on S2; frontend inbound 5xx max **98593**); cart/checkout hot-edge retries stayed flat at S1 leftovers. TopFull threshold stayed **10000**. Spec: [2026-09-15-s1-s2-baseline-metric-readout.md](docs/superpowers/specs/2026-09-15-s1-s2-baseline-metric-readout.md). YAMLs bumped to S1 **run22** / S2 **run21**.
- **Worker resized to e2-standard-16 + S1/S2 pair (2026-09-15)** — `topfull-worker-1` → **e2-standard-16** (master/load stayed e2-standard-8). Ran S1 baseline **run22**, S2 baseline **run21**, S2 RetryGuard **run10** (5 min cool-offs). **TopFull activates** on all three: frontend CPU max **883–977 m** (>800 m gate); `num_agent` non-zero; Layer A thresholds leave 10000. S2-base getcart goodput **~101** vs **~5** on e2-8 run17; P95 **~633 ms** vs **~2160 ms**. S2-RG still **zero ON→OFF** — TopFull absorbs overload at the proxy so mesh inbound rejection rarely stays above Threshold. S2-base run21 outbound Envoy `retry` Δ≈0 (no storm under active TopFull + 500 ms perTryTimeout). S6 YAMLs bumped to **run4**. Next free YAML slots: S1 baseline **run23**, S2 baseline **run22**, S2 RG **run11**. VMs **TERMINATED** after the sequence.
- **TopFull-off S2 control (2026-09-15)** — added optional `topfull_rl.enabled` (default **true**) to [`run_scenario.py`](experiments/run_scenario.py); existing 16 YAMLs unchanged. New standalone config [`scenario_2_baseline_no_topfull.yaml`](experiments/configs/scenario_2_baseline_no_topfull.yaml) (`log_folder: baseline_no_topfull_sustained_overload_run1`, independent of the `baseline_topfull_*` run counter). On e2-16, same S2 load as run21: **threshold stayed 10000 for all samples**, no `num_agent.csv`; outbound Envoy `retry` Δ **+211** (frontend→cart +202) vs **0** on TopFull-on run21; frontend inbound resets **+442** vs **+163**. Confirms TopFull admission suppresses retries on this machine. Retry volume is still far below e2-8 run20's storm (+166k) — e2-16 has more headroom even without RL. YAML bumped to **run2**. VMs **TERMINATED**.
- **RetryGuard perf fix + S2 re-run post-fix (2026-09-15, Tasks 1–3)** — `retryguard.py` replaced polling `service_inbound.csv` with `InboundCsvTailer` (append-only tail, never re-reads old rows); `resource_usage_collector.py` now adds a `__master_node__` row each poll via `/proc/stat` on master. Validated with unit tests. Fresh S2 runs on e2-16 after the fix: **S2 baseline run22** (getcart mean P95 756 ms) + **S2 RetryGuard run11** (getcart mean P95 1594 ms). Key results: (a) **no restart signature** in run11 — clean single START/SHUTDOWN per log; (b) **master CPU overhead of RG: +172 m max / +39 m mean** (~0.3% of 8-vCPU machine, negligible); (c) **P95 gap persists** (~2× higher with RG vs baseline) because RG never disables (zero ON→OFF events); only 6 OBSERVE lines with rejection > 0 across 5553 observations, all at SHUTDOWN moment. Root cause unchanged: TopFull Layer A throttles before mesh inbound sees sustained 5xx. S2 baseline YAML bumped to **run23**, RG YAML to **run12**. VMs stopped. Detail: [2026-09-15-TOPFULL-E2-16-RETRY-SUMMARY.md §8](Guides%20and%20Info/2026-09-15-TOPFULL-E2-16-RETRY-SUMMARY.md).
- **S3 targeted-bottleneck pair on e2-16 (2026-09-15)** — 300 s cool-off before first run and 300 s between arms. **S3 baseline run8** (getcart rows 505, mean P95 ~807 ms) + **S3 RetryGuard run7** (getcart rows 516, mean P95 ~751 ms). Full collector set both folders under `campaign_48/S3_targeted_bottleneck/` (mesh inbound/edges, resource_usage with `__master_node__`, Layer A/B). **RetryGuard did disable:** `retryguard.log` has **2× ON→OFF** and **2× OFF→ON** on `checkoutservice` (rejection reached 0.48 / 0.29 with `consecutive_high=30`). Contrasts with flat S2 where RG never fired under live TopFull. S3 YAMLs bumped to baseline **run9** / RG **run8**. VMs left **RUNNING**.
- **ρ-estimate report + auto-pull tooling (2026-09-16)** — new `experiments/rho_estimate_report.py` (importable `generate_report(run_dir)` + CLI) combines `estimate_service_mu.py`'s per-service λ/μ̂/ρ table with a `retryguard.log` `ON→OFF`/`OFF→ON` toggle summary into `rho_estimate_report.md`+`.json`, written **into the run folder itself**; degrades gracefully (no exception, just a noted reason) when `service_inbound.csv`/`topfull_detect.csv` (pre-2026-09-09 folders) or `retryguard.log` (baseline runs) are missing. New `experiments/pull_results.py <config.yaml>` runs the `scp -r` pull `run_scenario.py` currently only prints (reusing `run_scenario.scenario_dir_name()`, not duplicating the scenario→subfolder map) then auto-invokes the report — local/read-only w.r.t. the VMs, never touches `topfull-master`'s live pipeline; `--no-report`/`--dry-run` flags available. New project-local Cursor skill at `.cursor/skills/rho-estimate-report/SKILL.md` wraps the same logic for on-demand use. Unit tests in `experiments/test_rho_estimate_report.py` (58/58 passing across the three test modules) cover graceful degradation on folders missing `retryguard.log` and/or the mesh/detect CSVs. **Accuracy finding carried into every generated report:** re-reading `RetryGuard.pdf` §5 (ρ=λ/μ assumes a single-queue M/M/1/m per downstream service, Poisson arrivals, exponential service time) and `topfull_throttle_collector.py` (`utilization = cadvisor_cpu / quota`, a fixed hardcoded paper-limit denominator, not an enforced K8s limit) confirms `estimate_service_mu.py`'s CPU-linear μ̂ estimator only *approximates* the paper's ρ, and does so unevenly: on the one checkpoint folder with both `mu_cpu` and `mu_sat` (S3 RetryGuard `run7`, `frontend`), CPU-linear μ̂ overestimated capacity by >2x versus the saturated-goodput estimator (`mu_cpu=828` vs `mu_sat=390` at `util_peak≈1.0`) — diverging most exactly at the saturation regime RetryGuard's threshold exists to catch. It still worked directionally on `checkoutservice` in that same run (`rho_cpu_median=1.829`, correctly >1, coinciding with RG's real `ON→OFF` toggle at `rejection=0.48`), so it is **retained as a supplementary, directional diagnostic** — not a validated ground-truth ρ and not what `retryguard.py`'s live controller actually acts on (that stays the mesh rejection-rate surrogate, unchanged). No existing run result files were modified; the scratch-copy demo used a temp directory, not a tracked `experiments/results/` folder.
- **ρ estimator correction (2026-09-16)** — the `estimate_service_mu.py` "Estimator B" above (`mu_cpu = lambda / utilization`, from `topfull_detect.csv`'s CPU-quota bookkeeping) was a mistake: `utilization` is TopFull's own admission-control signal, not the RetryGuard paper's queueing-theoretic `rho = lambda/mu` (Sec 5, M/M/1(/m)). It is **removed entirely**, not kept as a fallback. Re-read `RetryGuard.pdf` in full for this: the paper names its stable-load model as M/M/1/m (Sec 5.2) but never describes estimating `mu` from a live system — `lambda`/`mu`/`m`/`k` are simulation/analytical parameters there, and Sec 4.3.1/Sec 6.2 confirm a live deployment should use a rejection-rate/retry-volume/delay **surrogate**, not a computed `rho` (which is exactly what `experiments/retryguard.py`'s live controller already does — unaffected by this correction). The corrected estimator is `mu_hat_w = lambda + 1/W` (M/M/1 steady-state sojourn-time relation, rearranged), using a genuinely per-service latency signal: `experiments/envoy_retry_collector.py` gained two **additive** trailing columns on both `service_edges.csv` and `service_inbound.csv` — `rq_time_sum_ms`/`rq_time_count`, from Envoy's `upstream_rq_time`/`downstream_rq_time` histogram `_sum`/`_count` series (existing columns/timing/behavior unchanged; confirmed `retryguard.py`'s `InboundCsvTailer` reads by column name, not position, so this cannot break it). `service_inbound.csv` also stores `rq_time_buckets` (JSON of cumulative histogram `le` → count) so the estimator can report a per-tick **P50** (`w_p50_ms`) alongside the mean. `topfull_detect.csv` is no longer read by `estimate_service_mu.py` at all. The saturated-goodput cross-check (`mu_sat`) is unchanged. Runs pulled **before 2026-09-16 lack the new latency columns** — `mu_hat_w`/`rho_w` report as `n/a` with an explicit note on those, not a silently wrong number. Spec: `docs/superpowers/specs/2026-09-16-rho-estimator-correction.md`.
- **ρ estimator live verification (2026-09-16)** — confirmed against a live `/stats/prometheus` scrape on this cluster: **inbound** `envoy_http_inbound_<listener>_downstream_rq_time_{sum,count,bucket}` exists and populates under traffic; **outbound** `envoy_cluster_upstream_rq_time_*` does **not** appear (Istio minimal proxy-stats excludes per-cluster detailed latency — structural zero on `service_edges.csv` `rq_time_*` until a deliberate `proxyStatsMatcher` change, out of scope; estimator only needs inbound). End-to-end smoke (`campaign_48/_smoketest_rho_verify_20260916/`, ~2 min curl traffic): frontend ρ_w≈0.22 / W≈25 ms, backends ρ≪1 / W in single-digit ms — plausible under light load. Locust uses `constant_throughput(1)` (not Poisson) — team-discussion only, do not change load-gen without a separate decision. VMs left **RUNNING**.
- **S3→S1→S2 checkpoint sextet with ρ reports (2026-09-16 → 2026-09-17)** — on e2-16 worker, 300 s cool-off before first run and between every arm. Order: **S3 baseline run9** + **S3 RG run8** + **S1 baseline run24** + **S1 RG run7** + **S2 baseline run23** + **S2 RG run12**. Each folder pulled via `pull_results.py` with `rho_estimate_report.md`/`.json` (live `rq_time_*` columns). **S3 RG:** **8× ON→OFF** and **8× OFF→ON** on `checkoutservice` (rejection 0.37–0.69). **S1 RG:** zero toggles (PASS). **S2 RG:** still zero toggles under live TopFull (frontend CPU max 867–950 m). S1 baseline **run23** Locust truncated (~9 rows) — discarded; **run24** is the usable S1 baseline. YAMLs bumped to: S3 base **run10** / S3 RG **run9**, S1 base **run25** / S1 RG **run8**, S2 base **run24** / S2 RG **run13**. VMs **TERMINATED** after the sequence.
- **`rho_w` cannot be the paper’s ρ (2026-09-17)** — after that readout: frontend `rho_w` ≈ 0.99 on both S1 and S2. The M/M/1 inversion `mu_hat_w = lambda + 1/W` is algebraically **always &lt; 1** and flattens once λW ≫ 1; it contradicts RetryGuard.pdf §5 (`rho = lambda/mu` may exceed 1) and S2’s “offered ρ > 1” language. Agreed diagnostic target (not implemented): S1 bottleneck ρ ≈ 0.5–0.8, S2 entry ρ > 1, not ρ > 1 on every service. Preferred later approach: freeze an independent μ, use offered λ (`ρ̂ = λ_offered / μ`); keep W as latency. Discussion: [2026-09-17-rho-paper-mismatch-discussion.md](docs/superpowers/specs/2026-09-17-rho-paper-mismatch-discussion.md). Controller unchanged.
- **`mu_hat_w`/`rho_w` removed entirely (2026-09-17)** — not just flagged as unreliable: `estimate_service_mu.py`/`rho_estimate_report.py` no longer compute or report them at all. Root cause is structural, not a precision issue: `W = 1/(mu-lambda)` presupposes `mu > lambda`, so solving for `mu` can only return a value just above the observed `lambda` — algebraically `rho_w = lambda/mu_hat_w = L/(L+1)` (`L = lambda*W`, Little's Law mean concurrency), always `< 1` and unable to represent `rho > 1`. It reported ~0.93 for `productcatalogservice` under healthy 6.9ms-latency S1 load, and disagreed with the one independent capacity cross-check available (`mu_sat`, S3 RG run8 frontend: `mu_hat_w`=371 / `mu_sat`=346, implying `rho`=0.98 vs 1.04). What remains in both files: `lambda_mean`, `w_mean_ms`/`w_p50_ms` (latency, not capacity), `mu_sat` (sparse, independent, capacity-only when real 5xx exists) — no `rho`/`mu.hat` field. All 8 stale `rho_estimate_report.md` files under `campaign_48/` regenerated to match. 36/36 unit tests updated and passing (`test_estimate_service_mu.py`, `test_rho_estimate_report.py`). Design for the replacement: freeze **throughput per millicore** (`mu_per_millicore`) from a dedicated calibration run, then `rho_hat = lambda_offered / mu_this_run` where `mu_this_run` is that constant times the evaluated run's Kubernetes CPU limit from **`service_capacity.json`** (not Locust user counts, not `topfull_run_quotas.json` — that file is TopFull's Detector overlay). Spec: [2026-09-17-frozen-capacity-rho-design.md](docs/superpowers/specs/2026-09-17-frozen-capacity-rho-design.md). **§7.1 / §7.3 decided** (same day): backend λ is first-attempt `service_edges.csv` `total` (retries stay a separate column); freeze μ for the **minimum set** only (`frontend`, `checkoutservice`, `productcatalogservice`, `paymentservice`) — other services stay `n/a`, not guessed. Controller (`retryguard.py`) unaffected.
- **Frozen-capacity ρ tools (2026-09-17)** — `experiments/capacity_frozen.py` and `experiments/rho_frozen_report.py` exist. Git-tracked `experiments/capacity/capacity_frozen.json` started as an uncalibrated skeleton; real μ is frozen as of the 2026-09-19 reset-driven bullet below. `pull_results.py` does **not** call the frozen report. `retryguard.py` unchanged. §7.2 / §7.4 / §7.5 / §7.6 locked at implementation: path `experiments/capacity/capacity_frozen.json`; `n_sat_ticks < 10` → `low_confidence`; hard error on `cluster_shape` mismatch; millicore-gap warn-and-rescale at ratio ≥ 2.0. §3.2 ramp-to-plateau is not implemented (v1 freeze is `mu_sat` only).
- **S2 frontend-2× / Locust user-scale investigation closed (2026-09-18)** — extra frontend CPU quota + more Locust users did **not** raise offered λ (closed-loop `constant_throughput(1)` + spawn divisor). Probe YAMLs, `online_boutique_create_probe.sh`, result folders, and the temporary `cpu_limit_fraction` cap of 4.0 were **removed**; S1–S6 runner behavior is back to pre-investigation (`cpu_limit_fraction` in `(0, 1]`). Shared `online_boutique_create.sh` was never patched. Keep the write-up only: [2026-09-17-s2-frontend-2x-quota-design.md](docs/superpowers/specs/2026-09-17-s2-frontend-2x-quota-design.md), [2026-09-17-s2-frontend-2x-user-scale-design.md](docs/superpowers/specs/2026-09-17-s2-frontend-2x-user-scale-design.md), [2026-09-18-locust-spawn-rate-fix.md](docs/superpowers/specs/2026-09-18-locust-spawn-rate-fix.md), [2026-09-18-s2-load-scaling-investigation-summary.md](docs/superpowers/specs/2026-09-18-s2-load-scaling-investigation-summary.md). Do not re-run this lever.
- **Frozen-capacity μ calibration battery (2026-09-19)** — best-effort TopFull-off + RetryGuard-off runs at full paper CPU on e2-16: frontend `baseline_no_topfull_sustained_overload_run2` (600 s); checkout+payment `calibration_checkout_payment_full_cpu_run1` (900 s, postcheckout=500); productcatalog `calibration_productcatalog_full_cpu_run1` (900 s, getproduct=800). 300 s cool-off between runs. Under the then-5xx-only `mu_sat` gate every target had `inbound_5xx_fraction=0` / `mu_sat=null` while inbound resets were high (same reset-driven pattern as S3 RG run8 checkout); freeze correctly refused at that moment. YAMLs bumped: no-TopFull **run3**, checkout/payment cal **run2**, productcatalog cal **run2**. Plan: [2026-09-18-frozen-capacity-calibration.md](docs/superpowers/plans/2026-09-18-frozen-capacity-calibration.md).
- **Reset-driven `mu_sat` freeze (2026-09-19)** — Tasks 1–2 gated `mu_sat` on `(Δ5xx+Δresets)/Δtotal` (`SAT_5XX_FRACTION` still 0.05). Freeze from the three full-CPU folders succeeded for all four minimum-set services (`method: mu_sat`): frontend `mu_per_millicore=0.238` (`baseline_no_topfull_sustained_overload_run2`, 1000m, `n_sat_ticks=2` low_confidence); checkoutservice `0.1615` and paymentservice `0.0195` (`calibration_checkout_payment_full_cpu_run1`, 1000m, 34 / 20 ticks); productcatalogservice `1.399` (`calibration_productcatalog_full_cpu_run1`, 500m, `n_sat_ticks=2` low_confidence). Did **not** freeze from S3 RG run8 (checkout 100m). Sanity `rho_frozen_report.py`: S1 baseline run24 frontend `rho_hat=1.632`; S2 baseline run23 frontend `rho_hat=2.316` (informal S1 0.5–0.8 band not met — frontend μ is 2-tick). Spec: [2026-09-19-reset-driven-mu-sat-design.md](docs/superpowers/specs/2026-09-19-reset-driven-mu-sat-design.md). Table: [experiments/capacity/README.md](experiments/capacity/README.md).
- **Constrained-CPU μ recalibration freeze (2026-09-19)** — after `SAT_TAIL_TRIM_TICKS=5` (unconditional last-5-row trim; `SAT_5XX_FRACTION` still 0.05; `LOW_CONFIDENCE_SAT_TICKS` still 10), re-froze frontend and productcatalog from constrained-CPU TopFull-off + RetryGuard-off runs. Frontend `mu_per_millicore=0.37` (`calibration_frontend_constrained_run1`, 100m, `n_sat_ticks=9` low_confidence — do not loosen the gate). Productcatalogservice `8.95` (`calibration_productcatalog_constrained_run1`, 50m, 60 ticks, note null). Checkoutservice `0.1615` and paymentservice `0.0195` left unchanged. Previous full-CPU 2-tick freezes (0.238 / 1.399) were shutdown-tail artifacts and must not be reused. Did **not** freeze frontend from the productcatalog folder. Sanity `rho_frozen_report.py`: S1 baseline run24 / S2 baseline run23 frontend `rho_hat` 1.05 / 1.49 (was 1.632 / 2.316); productcatalog 0.398 / 0.5418 (was 2.546 / 3.466); checkout 0.06168 / 0.1233 and payment 0.5108 / 1.021 unchanged. Informal S1 0.5–0.8 band still not met on frontend (9-tick freeze). Spec: [2026-09-19-frontend-productcatalog-mu-recalibration-design.md](docs/superpowers/specs/2026-09-19-frontend-productcatalog-mu-recalibration-design.md). Table: [experiments/capacity/README.md](experiments/capacity/README.md). **Historical:** do not reuse that 9-tick frontend freeze (superseded below).
- **Frontend μ 50m winner freeze (2026-09-19)** — replayed constrained frontend calibration at 100m (`calibration_frontend_constrained_run2`, `n_sat=16`) and 50m (`calibration_frontend_constrained_50m_run1`, `n_sat=26`). Winner = **50m** (higher `n_sat`). Re-froze frontend `mu_per_millicore=0.3` (`calibrated_at_cpu_limit_millicores=50`, `n_sat_ticks=26`, `note=null`). Checkout 0.1615 / payment 0.0195 / productcatalog 8.95 unchanged. Do **not** reuse run1 (0.37 / 9-tick) or freeze from 100m run2. Sanity `rho_frozen_report.py`: S1 baseline run24 / S2 baseline run23 frontend `rho_hat` 1.295 / 1.837 (was 1.05 / 1.49); checkout 0.06168 / 0.1233, payment 0.5108 / 1.021, productcatalog 0.398 / 0.5418 unchanged. Informal S1 0.5–0.8 band still not met. YAML next-free: 100m **run3**, 50m **run2**. Table: [experiments/capacity/README.md](experiments/capacity/README.md).
- **Payment + checkout constrained 50m freeze (2026-09-19)** — CPU-pegged TopFull-off + RetryGuard-off holds. Payment winner `calibration_payment_constrained_50m_run1`: `mu_per_millicore=0.72` (`calibrated_at_cpu_limit_millicores=50`, `n_sat_ticks=26`, peg cpu_max 49m of 50m, `note=null`). Checkout eligible `calibration_checkout_constrained_50m_run1`: `mu_per_millicore=10.82` (`calibrated_at=50`, `n_sat_ticks=210`, peg cpu_max 56m of 50m, `note=null`). Frontend 0.3 / productcatalog 8.95 unchanged. Do **not** reuse full-CPU checkout 0.1615 / payment 0.0195 (`calibration_checkout_payment_full_cpu_run1`, 1000m; payment never pegged). Did **not** freeze from S3 RG run8. Sanity `rho_frozen_report.py`: S1 baseline run24 / S2 baseline run23 frontend 1.295 / 1.837 and productcatalog 0.398 / 0.5418 unchanged; checkout 0.0009206 / 0.00184 (was 0.06168 / 0.1233); payment 0.01384 / 0.02764 (was 0.5108 / 1.021). Checkout 10.82 vs historical 0.1615 is a linearity check only. Informal S1 0.5–0.8 band still not met. YAML next-free: payment 100m **run2**, payment 50m **run2**, checkout 50m **run2**, unused checkout 100m stays **run1**. Table: [experiments/capacity/README.md](experiments/capacity/README.md).
- **Ron Nezer setup vs workshop (2026-09-20)** — SSH audit of the copied VMs: we inherited his **machines and kubeadm cluster**, not his TopFull/Locust recipe. Live experiments use `/home/idozacharia/TopFull` (KAIST `0c7af21` + our path/Locust-2.x/quota-overlay patches). His working tree is still `/home/user/TopFull`: detector CPU table rewritten (checkout 4500m), `instance_scaling` `[8,1,…]`, split-cart `frontend.sh`, `GETPRODUCT=600`, Locust task-weight remix, `ronnezer/*` images cached but not deployed. Write-up: [RON-NEZER-SETUP-VS-WORKSHOP.md](Guides%20and%20Info/RON-NEZER-SETUP-VS-WORKSHOP.md). Do not point `run_scenario.py` at `/home/user/TopFull`.
- **Ron-Nezer base migration (2026-09-20 → 2026-09-21)** — CPU/replica/HPA layer and loadgen **shape** are applied into the workshop tree (trimmed millicores from his January YAML, frontend HPA `maxReplicas: 4`, independent per-tag Locust swarms via `online_boutique_create_v2.sh`, `@task()` weight remix patcher). v2's spawn math matches Ron's float `count / RATE` divisor (2026-09-22), not users/sec. Narrative + what was left out: [RON-NEZER-BASE-MIGRATION.md](Guides%20and%20Info/RON-NEZER-BASE-MIGRATION.md). ADRs: [0001](docs/adr/0001-ron-setup-as-experiment-base.md)–[0004](docs/adr/0004-loadgen-shape-not-literal-numbers.md). **Still open:** loadgen numbers, S1–S6 methodology rework, YAML rewiring, fresh campaign — [remaining-work tracker](docs/superpowers/specs/2026-09-21-s1-s6-loadgen-numbers-remaining-work.md). CPU millicores stay **provisional** pending Ron's reply.

### ❌ Not done yet — remaining work

> **Ron Nezer base migration (CPU/replica/HPA + loadgen shape done; numbers/S1–S6 still open).** Narrative: [RON-NEZER-BASE-MIGRATION.md](Guides%20and%20Info/RON-NEZER-BASE-MIGRATION.md). Design: [2026-09-20-ron-nezer-base-migration-design.md](docs/superpowers/specs/2026-09-20-ron-nezer-base-migration-design.md) + [2026-09-21-loadgen-shape-ron-migration-design.md](docs/superpowers/specs/2026-09-21-loadgen-shape-ron-migration-design.md). Checkboxes for what's left: [2026-09-21-s1-s6-loadgen-numbers-remaining-work.md](docs/superpowers/specs/2026-09-21-s1-s6-loadgen-numbers-remaining-work.md). CPU millicores stay provisional until Ron replies ([EMAIL-DRAFT-RON-TOPFULL-LOAD.md](Guides%20and%20Info/EMAIL-DRAFT-RON-TOPFULL-LOAD.md)); if he names different live numbers, redo only the source table + trim arithmetic. **Do not** adopt `ronnezer/*` images, the 2600m ResourceQuota, or either of his launcher scripts' literal user counts. Scenario YAMLs still point at the legacy `create.sh`/`create2.sh` pair.
>
> **Next session — do this first:** S3 RG still toggles strongly on e2-16 (run8: 8× disable/re-enable at checkout); flat S2 RG still never disables under live TopFull (run12). Open questions: (1) S4 topology sensitivity; (2) whether a large retry storm on unconstrained e2-16 needs tighter timeout or TopFull-off control. Uniform Locust user-scale / frontend-2× quota is **closed** — do not add more Locust users or more loadgen VMs for that lever (write-up only: [2026-09-18-s2-load-scaling-investigation-summary.md](docs/superpowers/specs/2026-09-18-s2-load-scaling-investigation-summary.md)). Remaining S2 load-intensity options need a team decision: Locust `wait_time` (currently `constant_throughput(1)`, closed-loop), mix reweight, or TopFull-off. Shared `online_boutique_create.sh` still has `count/RATE` — leave it unless the team deliberately wants a global spawn change. Share [MENTOR-UPDATE.md](Guides%20and%20Info/mentor-update/MENTOR-UPDATE.md) with mentors. Then write Phase 7 eval on **`experiments/results/campaign_48/`** once mentors say what's sufficient. Do not overwrite `august_38/` or S3 run8/run7/run9, S1 run22/run24, S2 run22/run21/run10/run11/run23/run12, or `baseline_no_topfull_*_run1`. Discarded Locust-truncated folder: S1 baseline **run23**.
>
> **§7b / RetryGuard follow-up:** On e2-16 S2 RG run10/run11/run12, still zero disables (TopFull upstream of mesh). S3 run7/run8 **did** disable at checkout — controller path works when the bottleneck is a constrained backend. Decide whether to continue with S4 or retune S2. Do not overwrite those folders.
>
> **S6 YAMLs are already at run4.** Next free YAML slots: S1 baseline **run25**; S1 RetryGuard **run8**; S2 baseline **run24**; S2 RetryGuard **run13**; S3 baseline **run10**; S3 RetryGuard **run9**; no-TopFull control YAML is **run3**; checkout/payment full-CPU cal **run2**; productcatalog full-CPU cal **run2**; `calibration_frontend_constrained` is **run3**; `calibration_frontend_constrained_50m` is **run2**; `calibration_productcatalog_constrained` stays at **run1**; `calibration_payment_constrained` **run2**; `calibration_payment_constrained_50m` **run2**; `calibration_checkout_constrained_50m` **run2**; unused `calibration_checkout_constrained` stays **run1**. Other S4/S5 YAMLs still sit on the next free campaign-style slot (run7 / S5 run6) unless a later session bumped them.
>
> **Chart follow-up (do eventually):** `mentor_charts.py` / `mentor_charts_data.py` still only know Locust. Mesh `service_inbound.csv` / `service_edges.csv` are already meant as per-service **outcomes / insights**, not controller-only. Wiring them into plots is remaining work — do not treat “charts are Locust-only today” as “mesh is not an outcome.”
>
> **SLO-Fail / μ estimator:** Locust `Fail` is a 1 s SLO-miss, not Boutique 5xx — Guides updated; `python experiments/estimate_service_mu.py <run_dir>` is the offline per-service λ/W/mu_sat script ([2026-09-11-slo-fail-and-mu-estimator-design.md](docs/superpowers/specs/2026-09-11-slo-fail-and-mu-estimator-design.md)). **`mu_hat_w`/`rho_w` were removed entirely (2026-09-17), not just flagged** — circular by construction, see [2026-09-17-frozen-capacity-rho-design.md](docs/superpowers/specs/2026-09-17-frozen-capacity-rho-design.md) (supersedes [2026-09-17-rho-paper-mismatch-discussion.md](docs/superpowers/specs/2026-09-17-rho-paper-mismatch-discussion.md)'s "Option A" as a worked design). That doc now freezes `mu_per_millicore` (throughput per Kubernetes millicore from `service_capacity.json`) and sets `rho_hat = lambda_offered / mu_this_run`; millicore source is the kubectl snapshot, not TopFull's quota overlay. **§7.1 / §7.3 closed:** backend λ = first-attempt `service_edges.csv` `total`; freeze μ for `frontend` / `checkoutservice` / `productcatalogservice` / `paymentservice` only. **§7.2 / §7.4 / §7.5 / §7.6 locked at implementation:** path `experiments/capacity/capacity_frozen.json`; `n_sat_ticks < 10` → `low_confidence`; hard error on `cluster_shape`; millicore-gap warn-and-rescale at ratio ≥ 2.0. **2026-09-19 reset-driven freeze wrote `method: mu_sat` for all four**; same-day constrained-CPU recals replaced productcatalog (`8.95` at 50m, 60 ticks), frontend from the 50m winner (`0.3` at 50m, `n_sat_ticks=26`, `note=null` — do not reuse the 9-tick 0.37 / 100m run1 freeze), then payment (`0.72` at 50m, 26 ticks, CPU-pegged) and checkout (`10.82` at 50m, 210 ticks, CPU-pegged). Do **not** reuse full-CPU checkout 0.1615 / payment 0.0195. `pull_results.py` does not call the frozen report. Do not freeze from S3 RG run8 and do not lower `SAT_5XX_FRACTION` or `LOW_CONFIDENCE_SAT_TICKS`.

Optional: stop the 3 VMs when not analysing (`gcloud compute instances stop …`) to save cost.

### ⚠️ Live infra caveat (check before assuming the cluster is healthy)

The 3 VMs can be stopped/started between sessions (to save cost) and IPs are ephemeral. Last time this was checked (see §7), the VMs were `RUNNING` but `kubectl` on master returned `connection to localhost:8080 refused` — the API server hadn't come back up yet after a restart, even though `kubelet`/`docker`/`cri-docker` services were all `active`. This is most likely just "static pods still booting" — **always re-verify cluster health at the start of a session** rather than assuming it's still in the last-known-good state:

```powershell
ssh topfull-master "kubectl get nodes; kubectl get pods -n default; kubectl get virtualservices -n default"
```

If it doesn't recover within a few minutes, follow the troubleshooting table in SETUP-GUIDE.md / CONNECT-VMS.md. **Correction to that earlier diagnosis:** the actual cause turned out to be a missing `~/.kube/config` for the SSH user, not the API server booting — see the multi-user note in §7.

---

## 5. The scenarios (quick reference — full detail in SCENARIOS-GUIDE.md / PHASE5-EXPERIMENTS-GUIDE.md)

| # | Name | What changes | Duration | Config files |
|---|---|---|---|---|
| 1 | Normal Operation | Flat load, well within capacity | 5 min | `scenario_1_{baseline,retryguard}.yaml` |
| 2 | Sustained Overload (core) | Peak from t=0, **hold flat** | 10 min | `scenario_2_{baseline,retryguard}.yaml` |
| 3 | Targeted Bottleneck | `checkoutservice` at `cpu_limit_fraction: 0.1` (paper 1000m → **100m**) | 10 min | `scenario_3_{baseline,retryguard}.yaml` |
| 4A/4B | Topology Position | productcatalog (A → **50m**) vs payment (B → **100m**) at fraction 0.1 | 10 min | `scenario_4{a,b}_{baseline,retryguard}.yaml` |
| 5 | Interval Tuning | Same load as S6; `interval_samples` = 10/20/30/60 | 15 min | `scenario_5_interval_{10,20,30,60}s.yaml` |
| 6 | Forced Recovery | Peak 5 min, then ~25% load for 10 min | 15 min | `scenario_6_recovery_{baseline,retryguard}.yaml` |

Note: since all Boutique services run at **1 replica**, Scenarios 3/4 constrain via `method: cpu_limit` + **`cpu_limit_fraction: 0.1`** (not absolute `"100m"`), and the runner reconciles K8s + Detector to paper quotas before/after each run — a leftover checkout `100m` from an older S3/S4 is healed and must not be treated as the S2 baseline. New S4A (**50m**) is not comparable to `campaign_48/` S4A (absolute 100m). Scenario 5's 8 August matrix runs (`august_38/`) have **no re-enable events** (they used a flat hold). Campaign S5 (`campaign_48/`, S6's load) **did** re-enable; compare against **S6 baseline**, not S2. August Scenario 2 run1–3 remain historical; campaign S2 run4–6 is the primary flat-hold dataset.

---

## 6. How to run an experiment (once cluster is confirmed healthy)

```powershell
pip install pyyaml   # once

# Before every run: clear stale /tmp runner scripts.
# /tmp has a sticky bit — if a previous run was by a different Linux user, their
# files block SCP uploads. sudo rm is a no-op when files don't exist.
ssh topfull-master "sudo rm -f /tmp/rg_proxy.sh /tmp/rg_rl.sh /tmp/rg_mc.sh /tmp/rg_retryguard.sh /tmp/rg_envoy_retry.sh /tmp/rg_resource_usage.sh /tmp/rg_topfull_throttle.sh /tmp/rg_locust_launch.sh /tmp/envoy_retry_params.json"

python experiments/run_scenario.py experiments/configs/scenario_1_baseline.yaml
```

The runner handles everything end-to-end, including copying `retryguard.py`, `envoy_retry_collector.py`, `resource_usage_collector.py`, and `topfull_throttle_collector.py` from this repo onto master before those processes start. Results go to `results_base_path/<log_folder>/` **on master**. The runner's printed `scp` command already routes to the correct `campaign_48/<scenario_subfolder>/` (e.g. `S2_sustained_overload/`) based on the scenario — see [experiments/results/campaign_48/README.md](experiments/results/campaign_48/README.md).

**Before repeating a scenario:** bump `run_number` and change `log_folder` in the YAML, or the new run will overwrite the folder **on master**. Completed campaign slots are run4–6 (S1–S4), run3–5 (S5), run1–3 (S6). Post-campaign checkpoints (usable): S1 baseline **run24** (YAML now **run25**), S1 RG **run7** (YAML **run8**), S2 baseline **run23** (YAML **run24**), S2 RG **run12** (YAML **run13**), S3 baseline **run9** (YAML **run10**), S3 RG **run8** (YAML **run9**); earlier e2-8 / §7b / Locust-truncated S1 run23 folders remain historical. S6 YAMLs are at **run4**. Other S4/S5 YAMLs still point at the next free campaign-style slot (run7 / S5 run6) unless a later session bumped them. Never reuse August `run1–3` (S1–S4) or S5 `run1–2`.

Full config schema, `scale_constraints` methods, and troubleshooting: [PHASE5-EXPERIMENTS-GUIDE.md](Guides%20and%20Info/PHASE5-EXPERIMENTS-GUIDE.md) §5 and [experiments/README.md](experiments/README.md).

---

## 7. Infrastructure facts an agent needs

- **Project:** `networks-workshop` · **Zone:** `us-central1-a`
- **VMs:** `topfull-master` (K8s control plane + Istio control plane + TopFull proxy/RL/metrics/RetryGuard host), `topfull-worker-1` (all Online Boutique pods, 2/2 with Envoy sidecar), `topfull-load` (Locust only)
- **SSH:** always use the OpenSSH host aliases (`topfull-master`, `topfull-worker-1`, `topfull-load`) — **never hardcode IPs**. Full setup/repair playbook: [CONNECT-VMS.md](Guides%20and%20Info/CONNECT-VMS.md) (also enforced as an always-applied Cursor rule). Use `-o ControlMaster=no` for non-interactive commands.
- **IPs are ephemeral** — they change every time a VM is stopped/started. Refresh `HostName` in `~/.ssh/config` after every restart:
  ```powershell
  gcloud compute instances list --project=networks-workshop --format="table(name,status,networkInterfaces[0].accessConfigs[0].natIP)"
  ```
- **`infra/vm-ips.env`** is a static snapshot (from a teammate, `SSH_USER=idozacharia`) — treat it as historical reference only, not a live source of truth. Your own `~/.ssh/config` (per CONNECT-VMS.md, discovered per-machine) is authoritative for your session.
- **Multi-user setup on `topfull-master`.** Several teammates have their own Linux accounts on master (`idozacharia`, `idoza`, `sagi1`, `sagi151ps`, `danielalfasi`, `galho`/`GalHo`, `yoavnm`, `user`). All Phase 5/6 tooling paths in the scenario YAMLs' `infra:` block live under **`idozacharia`**'s home (`/home/idozacharia/TopFull/...`, `/home/idozacharia/experiments/...`) — this is intentional and paths were **not** changed to keep configs stable across teammates. As of 2026-08-07, running `run_scenario.py` as any other user (e.g. `sagi1`) required three one-time fixes on master (already applied, run once — re-check after a VM rebuild/reimage, not after a normal stop/start):
  1. **Kubeconfig** — other users have no `~/.kube/config` by default (only whoever ran `kubeadm init`, i.e. `idozacharia`, has one). Fix: `sudo cp /home/idozacharia/.kube/config ~/.kube/config && sudo chown -R $(whoami) ~/.kube && chmod 600 ~/.kube/config`. Without this, `kubectl` fails with `connection to localhost:8080 refused` — **not** an API-server-down symptom, easy to misdiagnose as a cluster health issue.
  2. **Write access for non-owners** — `/home/idozacharia/experiments` and `/home/idozacharia/TopFull/TopFull_master/online_boutique_scripts/src/logs` were owner/group-only writable (`idozacharia`/`idoza`). `run_scenario.py` needs to create `experiments/results/<log_folder>/` and the TopFull processes need to write CSVs into `src/logs/`. Fixed with `sudo chmod o+w` on those two directories (not recursive — deliberately minimal).
  3. **`go` not on PATH for non-interactive shells** — `/usr/local/go/bin` was only added to `idozacharia`'s personal `~/.bashrc`, but `run_scenario.py` starts the proxy via a non-interactive shebang script (`tmux new-session -d ... /tmp/rg_proxy.sh`), which never sources `.bashrc` for *any* user (this would have silently broken the proxy step even for `idozacharia`). Fixed system-wide: `sudo ln -sf /usr/local/go/bin/go /usr/local/bin/go` (`/usr/local/bin` is in `/etc/environment`'s default `PATH`).

  Verified working end-to-end as `sagi1` after the fixes: `kubectl get nodes`, `go version`, write-tests on both directories, and `python3 -c "import ray, kubernetes"` inside `idozacharia`'s venv all succeed non-interactively.
- **Cost discipline:** stop the 3 VMs when not actively working: `gcloud compute instances stop topfull-master topfull-worker-1 topfull-load --zone=us-central1-a`. Don't stop them unless the user asks.
- **Known fixes already applied on master** (don't revert): dynamic cAdvisor IP lookup in `resource_collector.py`; Locust 2.x event API in `locust_online_boutique.py`; LF line endings + venv locust path in the create scripts; env-var-with-default user counts in create scripts; try/except wrapper in `metric_collector.py`. Full list in PHASE5-EXPERIMENTS-GUIDE.md §7. **Also (2026-08-07 readiness):** `retryguard.py` disable path omits the `retries` block (Istio rejects `attempts:0`). `run_scenario.py` copies `retryguard.py`, `envoy_retry_collector.py`, and `resource_usage_collector.py` from this repo onto master at the start of each run (plain `cp`, then `sudo cp` if the dest file is owned by another user). Do not skip that by launching those scripts by hand from a stale master copy.

---

## 8. Key open questions this project is trying to answer

(from [Evaluating_RetryGuard_on_TopFull.md](context/Evaluating_RetryGuard_on_TopFull.md) slides 8–9 / MENTOR-COORDINATION.md — keep these in mind when interpreting results. Answerability after the 2026-08-20 audit is in [PHASE7-DATA-GAPS.md](Guides%20and%20Info/PHASE7-DATA-GAPS.md).)

- Does RetryGuard help at the **system level**, the **per-microservice level**, or both? (Mentors expect system-level gain may be small (~0.2%) while specific services improve a lot (~30%).) — **answerable** (goodput, P95, rejection).
- **Topology beneficiaries** — which services gain most from suppressed retries? — **answerable** (per-endpoint CSVs).
- **Chain propagation** — does relief at a bottleneck propagate upstream to callers? — **answerable**, coarse (only 5 Locust endpoints).
- **Controller interaction** — how do TopFull's RL loop (1s) and RetryGuard (30s windows) interact/oscillate? — **partial** (`RPS` as proxy; `num_agent.csv` is empty). Recover→re-enable is the S6 load-drop (campaign complete), not flat S2.
- **Topology position sensitivity** — does RetryGuard help more where TopFull's entry-level signal is weaker (Checkout-mediated Payment vs direct ProductCatalog)? — **answerable** (S4A vs S4B).
- **Interval sensitivity** — does the paper's 30s default hold up when running alongside TopFull's 1s RL loop? — **answerable** on campaign S5 ×3 (`campaign_48/`, recovery load). August S5 remains a negative result (flat hold, never re-enabled).

---

## 9. Keeping this file useful

When you complete meaningful work (a scenario actually run, a bug fixed, a new phase closed), update §4 ("Current status") so the next agent doesn't have to re-derive it from git log and guide files. Keep this file's own content lean — link to the detailed guide instead of duplicating it.
