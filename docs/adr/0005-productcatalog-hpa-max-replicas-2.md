# Enable productcatalogservice HPA (maxReplicas: 2), accept the node-overcommit risk

`productcatalogservice` HPA was left disabled during the CPU/HPA migration ([ADR-0003](0003-fit-ron-footprint-on-16-vcpu-worker.md)) because it seemed to conflict with Scenario 4A's premise that productcatalog is a fixed, non-compensating bottleneck. Re-examined during the S1–S6 methodology rework: Kubernetes CPU limits are always per-pod-template, so the bottleneck cap (see [ADR-0006](0006-absolute-bottleneck-cap-not-fraction.md)) already applies to every productcatalog replica regardless of HPA — there is no real mechanism conflict, only a resource-budget one. We enable HPA on productcatalog **universally** (matching Ron's own setup — he had an HPA file for it too, same family as frontend's), capped at `maxReplicas: 2`, with **no pre-emptive CPU re-trim** of the other 10 services.

**Status:** proposed (design only — see [2026-09-21-s1-s6-methodology-and-calibration-design.md](../superpowers/specs/2026-09-21-s1-s6-methodology-and-calibration-design.md) §4).

**Considered options:** uniform re-trim of all 11 services (~89% of the current trimmed table) to guarantee headroom for a 2nd productcatalog replica under any combination of scenarios; lower frontend's own `maxReplicas` ceiling to free room instead; build a CPU-independent bottleneck mechanism (fault injection / circuit breaker) so HPA and the bottleneck cap can't interact at all.

**Consequences:** `topfull-worker-1`'s app-container budget is already at its edge at frontend's own full 4-replica scale-out (ADR-0003's ≈13,360m against a ≈13,350m budget). Adding productcatalog's 2nd replica (+1,535m app +100m sidecar) has **no guaranteed headroom** if frontend is simultaneously at its own max — an explicit, accepted overcommit risk, not an oversight. If both ever happen at once, new productcatalog pods can go `Pending` (silently unschedulable) rather than actually scaling out, which could look like "the bottleneck held" when it's really "the node ran out of room." **Action required once implemented**: note this risk in [RON-NEZER-BASE-MIGRATION.md](../../Guides%20and%20Info/RON-NEZER-BASE-MIGRATION.md) so a future agent debugging an unexplained "didn't scale" result checks node scheduling pressure (`kubectl describe node topfull-worker-1`) before assuming the scenario mechanism itself failed.

Detail: [2026-09-21-s1-s6-methodology-and-calibration-design.md](../superpowers/specs/2026-09-21-s1-s6-methodology-and-calibration-design.md) §4.
